# AJW SCM 업무 프로세스

(주)에이제이월드 SCM팀 — 업무 흐름 및 시스템 연계 구조

> 시스템 아키텍처·컴포넌트 구조는 **[ARCHITECTURE.md](./ARCHITECTURE.md)** 참조  
> 개발 컨벤션·탭 구조·핵심 타입은 **[CLAUDE.md](./CLAUDE.md)** 참조

---

## 전체 업무 흐름

```
판매 raw 파일
      │
      ▼
 판매현황 분석 ─────────────────────────────────────────────────┐
      │                                                          │
      ▼                                                          ▼
 STEP 2 (판매분석)                                        수입 발주계획
      │                                                          │
      ▼                                                          ▼
 STEP 1 (ERP 가공) ──→ STEP 3 (발주계획) → 생산자재 발주       수익성 분석
                                                          │
EMP 재고 ─────┐                                           │
이카운트 재고 ─┤──→ 재고 대사                              ▼
미출하현황 ────┤                                     월간 수익률 분석
입출고확인서 ──┘                                     (수익률 파일 자동 입력)
```

---

## 1. 생산자재 발주계획 (3-Step)

**목적**: 케이블·하우징·페룰 생산자재의 적정 발주량 산출  
**주기**: 월 1회 또는 분기별  
**담당**: SCM팀

### STEP 1 — ERP 파일 가공

| 항목 | 내용 |
|---|---|
| 입력 | ERP 생산실적 파일 (.xlsx) |
| 처리 | 품번 자동 감지, 연도별 피벗, 신규 품번 타입 분류 |
| 출력 | 가공파일.xlsx (다운로드) + Metadata 저장 |
| 주의 | 신규 품번 발견 시 수동 등록 후 재실행 필요 |

### STEP 2 — 판매 분석

| 항목 | 내용 |
|---|---|
| 입력 | 판매분석 파일 (.xlsx) |
| 처리 | 연도별 판매량 집계, CAGR 계산, 월간 최고치 추출 |
| 출력 | SalesAggResult 저장 (localStorage + Supabase) |

### STEP 3 — 발주계획 생성

| 항목 | 내용 |
|---|---|
| 입력 | 가공파일 + SalesAgg + 현재고 + 기발주 + 설정값 |
| 처리 | 안전재고·발주필요량 계산 |
| 출력 | 발주계획.xlsx (다운로드) → 구매팀 전달 |

**계산 공식**

```
안전재고   = 3개년 월간 피크평균 × (리드타임 / 30) × 안전계수(safetyK)
발주필요량 = max(0, 예측연간 + 안전재고 − 현재고 − 기발주)
```

**병목 지점**
- STEP 1 품번 누락 → 수동 등록 후 재실행
- 현재고·기발주 수치는 수동 입력 (ERP 자동 연동 없음)

---

## 2. 완제품 수입 관리

**목적**: 해외(FLC) OJC 제품의 적시 발주 및 입고 추적  
**주기**: 월 1회 + 상시 D-day 모니터링  
**담당**: SCM팀

### 2-1. 수입 발주계획 수립

| 항목 | 내용 |
|---|---|
| 입력 | 재고파일 (EMP 또는 이카운트, 자동 형식 감지) + 판매현황 |
| 처리 | OJC 품목 자동 필터, 커버리지·발주필요량 계산 |
| 출력 | 완제품수입발주계획.xlsx (다운로드) |

**계산 공식**

```
커버리지   = 현재고 ÷ 월간최고
발주필요량 = (월간최고 × 목표개월) − 현재고
상태 분류  = urgent(1개월 미만) / warning(2개월 미만) / ok / intermittent / no_data
```

**간헐적 수요 처리**
- 판매월수 < 3개월(기준값 조정 가능) → 발주계획에서 제외
- 별도 "간헐적 수요" 탭으로 분리하여 수동 판단

### 2-2. 발주현황 등록 및 D-day 관리

| 항목 | 내용 |
|---|---|
| 등록 단위 | 업체 + 연도 + 차수 (예: FLC 26-05차) |
| D-day | 예상 입고일 기준 자동 카운트 |
| 알림 기준 | 지연(음수) / 긴급(D-14 이내) / 대기(D-14 초과) |
| 입고 처리 | 실제 입고일 등록 → 리드타임 자동 계산 → 이력 누적 |

### 2-3. 수익성 분석

| 항목 | 내용 |
|---|---|
| 입력 | 맥산 생산원가 파일 + FLC 수입원가 파일 + 계약단가 파일 |
| 판매가 기준 | 계약단가 우선 → 없으면 판매이력 평균 |
| 원가 적용 | KT향 → 표준원가 / LG향·기타 → 생산원가 |
| 출력 | 수익성분석.xlsx (마진율·권고 테이블 포함) |

**계산 공식**

```
마진율       = (판매가 − 원가) / 판매가 × 100
생산유지기준 = max(20, 최소마진율 × 3)   ← 검토필요 구간 자동 확장
맥산 가중치  = 5% 추가 (기본 OFF, CEO 지시사항)
```

---

## 3. 재고 대사

**목적**: EMP(내부 WMS) ↔ 이카운트(ERP) 재고 불일치 모니터링  
**주기**: 주 1회 또는 월말  
**담당**: SCM팀

### 입력 파일 4종

| 파일 | 형식 | 비고 |
|---|---|---|
| EMP 재고현황 | .xls | WMS 기준 재고 |
| 이카운트 재고 | .xlsx (복수) | ERP 기준, E열(창고별 재고) 사용 |
| 미출하현황 | .xlsx | 이카운트에 반영 안 된 미출하 수량 |
| 일일입출고확인서 | .xlsx | EMP 출고 합산용 |

### 대사 공식

```
EMP조정 = EMP재고 − 출고합산(일일입출고확인서)
EC조정  = 이카운트재고 + 미출하
차이    = EMP조정 − EC조정
```

### 처리 흐름

```
파일 4종 업로드 (드래그앤드롭 지원)
      │
      ▼
대사 실행 → 품목별 차이 계산
      │
      ▼
차이 있는 품목 리스트 → 담당자 검토
      │
      ▼
이력 저장 → 추이 차트 누적
```

---

## 4. 월간 수익률 분석 (수익률 파일 자동 입력)

**목적**: 월간 OJC 판매 건별 맥산/수입 수량 구분 자동 입력  
**주기**: 월 1회  
**담당**: SCM팀 (기존 수동 작업 → 자동화)

### 입력 파일 3종

| 파일 | 역할 |
|---|---|
| 수익률 분석 파일 | 영업관리팀 작성, 판매 건 기준 파일 (기준) |
| 주문건 조정 파일 (미출하현황 1) | SCM팀 작성, 맥산/수입 구분 메인 소스 |
| 출하현황 파일 | 보조 검증, 조정 파일 미기재 건 fallback |

### 생산구분 판독 규칙

| 기재값 | 해석 |
|---|---|
| (공백) 또는 수입/FLC | 전량 수입 |
| 생, 셍 | 전량 맥산 |
| 생N (예: 생16) | 맥산 N개, 나머지 수입 |

### 매칭 상태 4종

| 상태 | 의미 | 색상 |
|---|---|---|
| ✅ match | 조정 파일 = 출하현황 일치 | 기본 |
| ⚠️ mismatch | 조정 파일 ≠ 출하현황 불일치 | 빨강 — 검토 필요 |
| 📋 adj_only | 조정 파일만 존재 (출하현황 없음) | 파랑 |
| 🔄 fallback | 출하현황만 존재 (조정 파일 누락) | 노랑 |
| ❌ none | 양쪽 모두 미매칭 | 회색 |

### 핵심 처리 로직

```
① 사전 스캔: 동일 (주문NO, 품목코드) 키의 총 수량 집계
② 비례 배분: 각 행의 수량 / 총 수량 비율로 맥산·수입 분배
③ 마지막 행: 남은 수량 전부 할당 (반올림 오차 흡수)
④ 행별 상한: 맥산 + 수입 ≤ 해당 행 수량
⑤ Excel 정밀도 보정: 16자리 주문NO 끝자리 0 → 1~9 대체 시도
```

### 출력

- 수익률분석 파일에 맥산수량·수입수량·검토사항 열 자동 입력
- 제품별 요약 시트 (KT/LG OJC SP·DP·MOJC·MM·OM3 세분류)
- 불일치 건 검토 탭 (인라인 수정 후 재다운로드)

---

## 5. 판매 현황 분석

**목적**: 연도별·품목별 판매 추이 및 CAGR 파악  
**주기**: 수시 (신규 판매 데이터 수신 시)  
**담당**: SCM팀

```
판매 raw 파일 업로드
      │
      ▼
연도 선택 (다중 선택 가능)
      │
      ▼
품목별 월간 판매량 집계 + CAGR 계산
      │
      ▼
판매분석.xlsx 다운로드
(→ STEP 2, 수익성 분석의 기초 데이터로 활용)
```

---

## 6. 품번 관리

**목적**: OJC 품번 및 EMP 로트코드의 일관된 생성·관리

### OJC 품번 체계

```
{타입}{코어타입}{코어수}{재질}{커넥터}{마킹}-{길이코드}
예: C-ALNN-3
```

- 자동 생성 (OJC 코드표 규칙 기반) + 수동 생성 지원
- 관리자 탭에서 코드표 편집 가능

### EMP 로트코드 체계

```
{기본코드} ({업체코드}-{YYMM}-{발주차수}_{입고회차})
예: 14-K-107 (FLC-2601-01_02)
```

---

## 데이터 연계 구조

```
[원천 데이터]          [저장소]              [활용처]

ERP 생산실적    →  Metadata (localStorage)  →  STEP 3 발주계획
판매 raw 파일   →  SalesAgg (localStorage)  →  STEP 3 / 수입발주계획
재고 파일       →  (파일 업로드, 비저장)    →  재고대사 / 수입발주계획
원가 파일       →  (파일 업로드, 비저장)    →  수익성 분석
발주 등록       →  ImportOrder (localStorage) →  D-day 모니터링 / 대시보드
대사 결과       →  ReconHistory (localStorage) → 추이 차트
품번 규칙       →  OjcRules (localStorage + Supabase) → 품번 생성기
```

---

## 현재 미구현 / 개선 여지

| 항목 | 현황 | 비고 |
|---|---|---|
| EMP 로케이션 KT향 | 탭 숨김 | KT향 로케이션 재구성 예정 |
| 리드타임 집계 분석 | 미구현 | 입고 이력 → 업체별 평균 LT 자동 계산 |
| 재고대사 → STEP 3 연동 | 수동 | 대사 결과 현재고 → STEP 3 자동 반영 가능 |
| 월간 수익률 → 수익성 분석 연동 | 별개 파일 | 동일 데이터 기반 통합 가능 |
| ERP 직접 연동 | 미구현 | 현재 파일 업로드 방식 |

---

*최종 업데이트: 2026-06-15*
